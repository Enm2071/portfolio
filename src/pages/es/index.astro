---
import Welcome from '../../components/Welcome.astro';
import Layout from '../../layouts/Layout.astro';

// Static across requests -- statically build it instead of paying the SSR
// cost (locale/data lookups, template render) on every crawl/visit.
export const prerender = true;
---

<Layout>
	<Welcome />
</Layout>
